# 本次更新说明

## 已修复

1. AI 行情问答会自动查询 Gate USDT 永续的标记价、24h 成交额、资金费率、全市场未平仓量（OI）以及 15m/4h K 线技术摘要。
2. 同一用户在同一频道 30 分钟内追问“成交量和持仓呢”等问题时，会沿用上一轮明确提及的币种，不再错误回退到 BTC/ETH。
3. OI 或某一周期 K 线接口失败时，已经成功取得的价格和其他数据仍会交给 AI，不会整轮回答失败。
4. 持仓卡片标题和顶部会醒目显示总未实现盈亏、总保证金、整体保证金收益率以及成功计价笔数。
5. 总收益会覆盖所有成功计价的仓位；明细仍最多展开 20 笔。部分仓位无法取价时会明确标记，避免把部分合计误认为全部收益。

## 更新方式

本次没有数据库结构变更，原来的 `data/` 数据可以继续使用。替换源码后重新构建并启动：

```powershell
docker compose up -d --build
docker compose logs -f bot
```

如果使用本地 Python：

```powershell
python -m pip install -e ".[dev]"
python -m crypto_bot.main
```

## 建议验收

在 AI 频道依次发送：

```text
LAB/USDT 的价格，并告诉我是否适合进多单
你再查询一下相关的成交量和持仓量
```

第二句应继续分析 LAB，并使用成交额、OI、资金费率和 K 线摘要。随后在持仓频道发送“收益多少”，卡片顶部应直接显示总收益。

说明：技术判断是基于 Gate 公开市场数据的条件式分析，不包含手续费、资金费、滑点，也不构成投资建议。
